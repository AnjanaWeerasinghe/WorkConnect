rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE - Allow all reads and writes
    // TODO: Replace with proper security rules for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}